"""MoveAny engine - a top-level, tool-agnostic package.

These are the reusable, GUI-agnostic building blocks. The CLI in
`moveany/cli.py` orchestrates them; the future GUI will import them directly.

Import directly from modules, e.g.:

    from modules import files
    from modules.files import files_identical
"""
