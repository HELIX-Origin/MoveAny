"""MoveAny - safely relocate folders whose files exceed path-length limits.

A modular Python CLI (and future GUI) for safely relocating directories,
batched per project, with full logging and verification.
"""

__version__ = "0.1.0"
